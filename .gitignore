# Environnement virtuel et cache Python
.venv/
venv/
__pycache__/
*.pyc

# Données et bases de données (trop lourdes pour GitHub)
data/
reports/calls.db

# Fichiers de configuration ou d'exécution temporaires
.pipeline_run.yaml

# Configuration secrète (si tu as mis ton HF_TOKEN dedans)
#config.yaml

# Cacher les fichiers de logs et les bases de données SQLite locales
logs/*.log
reports/calls.db

# Cacher UNIQUEMENT les fichiers d'appels temporaires (les "tmp")
reports/calls/CC-tmp*